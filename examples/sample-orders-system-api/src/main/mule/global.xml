<?xml version="1.0" encoding="UTF-8" ?>
<mule
    xmlns="http://www.mulesoft.org/schema/mule/core"
    xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
    xmlns:http="http://www.mulesoft.org/schema/mule/http"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
                          http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd"
>
    <configuration-properties file="${mule.env}.yaml" doc:name="Environment Properties" />

    <http:listener-config name="http-listener-config" doc:name="HTTP Listener">
        <http:listener-connection host="${http.host}" port="${http.port}">
            <reconnection>
                <reconnect-forever frequency="5000" />
            </reconnection>
        </http:listener-connection>
    </http:listener-config>

    <http:request-config
        name="orders-backend-request-config"
        doc:name="Orders Backend"
        responseTimeout="${orders.backend.responseTimeout}"
        connectionIdleTimeout="30000"
    >
        <http:request-connection
            protocol="HTTPS"
            host="${orders.backend.host}"
            port="${orders.backend.port}"
            maxConnections="20"
            connectionIdleTimeout="30000"
        >
            <reconnection>
                <reconnect count="3" frequency="2000" />
            </reconnection>
        </http:request-connection>
    </http:request-config>

    <error-handler name="global-error-handler" doc:name="Global Error Handler">
        <on-error-propagate type="HTTP:CONNECTIVITY" doc:name="Backend Unavailable">
            <set-variable variableName="httpStatus" value="503" doc:name="Set Status" />
            <logger
                level="ERROR"
                category="com.example.orders.error"
                message='#[%dw 2.0 output application/json --- {message: "Backend unavailable", correlationId: correlationId}]'
                doc:name="Log Connectivity Error"
            />
        </on-error-propagate>
        <on-error-propagate type="ANY" doc:name="Unexpected Error">
            <set-variable variableName="httpStatus" value="500" doc:name="Set Status" />
            <logger
                level="ERROR"
                category="com.example.orders.error"
                message='#[%dw 2.0 output application/json --- {message: error.detailedDescription, correlationId: correlationId}]'
                doc:name="Log Unexpected Error"
            />
        </on-error-propagate>
    </error-handler>
</mule>
