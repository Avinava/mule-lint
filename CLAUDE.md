# Claude compatibility note

Read and follow [AGENTS.md](AGENTS.md); it is the canonical repository guidance.

Essential commands:

```bash
npm ci
npm run build
npm test
npm run check
mkdocs build --strict
```

For rule work, also follow [.agent/workflows/add-rule.md](.agent/workflows/add-rule.md). Do not use private Mule projects or generated customer reports as committed fixtures, examples, or screenshots.
