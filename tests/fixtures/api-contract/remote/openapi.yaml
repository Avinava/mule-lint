openapi: 3.0.3
info:
  title: Remote Reference API
  description: Proves that implicit network references are rejected.
  version: 1.0.0
paths:
  /items:
    get:
      operationId: listItems
      description: Lists items.
      responses:
        '200':
          description: Items were returned.
          content:
            application/json:
              schema:
                $ref: 'https://example.invalid/schemas/item.json'
